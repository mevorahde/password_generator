Password Generator script originally created by CuriousCodingYT

Parms set as followed:

Length of the password: "--length", "-L"
How many passwords to generation: "--number", "-N"
Should the password have symbols: "--symbols", "-S"
